import logo from "../logo.svg";

const ReactLogo = () => {
  return (
    <img
      src={logo}
      className="App-logo"
      alt="react-logo"
      style={{ height: "5rem" }}
    />
  );
};

export default ReactLogo;
